---
// Myservice.astro — same fade-in on scroll as your About section
---

<section id="myservice" class="services">
  <div class="container">
    <p class="eyebrow reveal" style="--d:.00s">Our Services</p>

    <h2 class="reveal" style="--d:.05s">Web Design and Development Services</h2>

    <p class="subhead reveal" style="--d:.10s">
      At Stone Digital, we specialise in high-quality web design and development tailored to your
      unique needs. Our expert team delivers custom solutions across the leading web platforms:
      WordPress, WooCommerce, and Shopify.
    </p>

    <div class="cards">
      <!-- WordPress -->
      <article class="card reveal" style="--d:.20s">
        <div class="iconwrap">
          <svg width="32" height="32" viewBox="0 0 24 24" aria-hidden="true" class="icon">
            <circle cx="12" cy="12" r="10"></circle>
            <path d="M2 12h20M12 2c3.5 3.2 3.5 16.8 0 20M12 2c-3.5 3.2-3.5 16.8 0 20" fill="none" stroke="#141414" stroke-width="1.5"></path>
          </svg>
        </div>
        <h3>Custom WordPress Development</h3>
        <p>
          We offer custom WordPress solutions to create powerful and user-friendly websites that
          meet your business goals.
        </p>
      </article>

      <!-- Shopify -->
      <article class="card reveal" style="--d:.28s">
        <div class="iconwrap">
          <svg width="32" height="32" viewBox="0 0 24 24" aria-hidden="true" class="icon">
            <path d="M7 7V6a5 5 0 0 1 10 0v1h2a1 1 0 0 1 1 1v11a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V8a1 1 0 0 1 1-1h2zm2 0h6V6a3 3 0 0 0-6 0v1z"></path>
          </svg>
        </div>
        <h3>Shopify Development</h3>
        <p>
          With our expertise in Shopify development, we create stunning online stores that drive
          sales and enhance user experience.
        </p>
      </article>

      <!-- WooCommerce -->
      <article class="card reveal" style="--d:.36s">
        <div class="iconwrap">
          <svg width="32" height="32" viewBox="0 0 24 24" aria-hidden="true" class="icon">
            <path d="M7 18a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm10 0a2 2 0 1 0 0 4 2 2 0 0 0 0-4zM6.2 6h13.3l-1.2 6.4a2 2 0 0 1-2 1.6H9.1a2 2 0 0 1-2-1.6L5.4 3H3V1h3a2 2 0 0 1 2 1.7L8.5 6z"></path>
          </svg>
        </div>
        <h3>WooCommerce Development</h3>
        <p>
          Our team specialises in WooCommerce development, helping you build robust and scalable
          e-commerce solutions.
        </p>
      </article>
    </div>

    <div class="actions reveal" style="--d:.44s">
      <a href="#contact" class="btn btn-primary">Contact Us</a>
      <a href="#learn-more" class="btn btn-secondary">Learn More</a>
    </div>
  </div>

  <style>
    /* ===== Fade-in on scroll (same as About) ===== */
    .reveal{
      opacity:0;
      transform:translateY(14px);
      transition:
        opacity .7s cubic-bezier(.22,.61,.36,1) var(--d,0s),
        transform .7s cubic-bezier(.22,.61,.36,1) var(--d,0s);
      will-change: opacity, transform;
    }
    .reveal.is-inview{ opacity:1; transform:none; }
    @media (prefers-reduced-motion: reduce){
      .reveal, .reveal.is-inview{ opacity:1 !important; transform:none !important; transition:none !important; }
    }

    /* Layout & colors only (no font-size/weight) */
    .services { background: #141414; color: #ffffff; }
    .container { max-width: 1120px; margin: 0 auto; padding: 64px 24px; text-align: center; }
    .eyebrow { color: #a3a3a3; margin: 0; }
    .subhead { color: #cfcfcf; margin: 16px auto 0; max-width: 820px; }

    .cards { display: grid; gap: 24px; margin-top: 40px; grid-template-columns: 1fr; }
    @media (min-width: 900px) { .cards { grid-template-columns: repeat(3, 1fr); } }

    .card {
      background: #1e1e1e;
      border: 1px solid #2a2a2a;
      border-radius: 16px;
      padding: 24px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.35);
    }
    .card p { color: #b3b3b3; }

    .iconwrap { display: flex; justify-content: center; margin-bottom: 16px; }
    .icon { display: block; fill: #ffffff; }
    .services .icon { width: 32px; height: 32px; } /* prevent global svg:100% rules */

    .actions { margin-top: 40px; display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
    .btn { padding: 12px 20px; border-radius: 8px; text-decoration: none; border: 1px solid transparent; }
    .btn-primary { background: #d9d9d9; color: #000; border-color: #d9d9d9; }
    .btn-secondary { background: transparent; color: #fff; border-color: #6b6b6b; }
  </style>

  <!-- Tiny JS to toggle .is-inview for this section only -->
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      var section = document.getElementById('myservice');
      if (!section) return;

      var els = section.querySelectorAll('.reveal');
      if (!('IntersectionObserver' in window)) {
        for (var i = 0; i < els.length; i++) els[i].classList.add('is-inview');
        return;
      }
      var io = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-inview');
            io.unobserve(entry.target);
          }
        });
      }, { threshold: 0.12 });

      for (var i = 0; i < els.length; i++) io.observe(els[i]);
    });
  </script>
</section>
